{
  "name": "gta5mini",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Browser-based FPS prototype: modern rendering pipeline, zero external assets.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "shots": "node tools/shoot.mjs",
    "probe": "node tools/probe-exposure.mjs",
    "boot": "node tools/boot-check.mjs"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "playwright": "^1.62.1",
    "vite": "^7.1.0"
  }
}
